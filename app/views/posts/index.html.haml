- @posts.each do |post|
  = link_to (image_tag post.image, {:class => "img_small"}), post
  %h2= link_to post.title, post
  %p= pluralize(post.comments.count, "comment")
  %p= pluralize(post.get_likes.size, "like")
= link_to "Add New Inspiration", new_post_path
